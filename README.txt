Author: Manny Fluss

Instructions:
    -install pandas using the terminal command
    -type pip install -r requirements.txt
    -if that gets an error
        -type pip install pandas
        -type pip install openpyxl
    
under development
